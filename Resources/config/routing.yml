erichard_glide_resize:
    path:     /{server}/{path}.{_format}
    defaults: { _controller: Erichard\GlideBundle\Controller\GlideController::resizeAction }
    methods: [GET]
    requirements:
        path: ".+"
        _format: ((?i)jpe?g|png|gif)
