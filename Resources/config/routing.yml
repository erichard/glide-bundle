erichard_glide_resize:
    path:     /{server}/{path}.{_format}
    defaults: { _controller: Erichard\Bundle\GlideBundle\Controller\GlideController::resize }
    methods: [GET]
    requirements:
        path: ".+"
        _format: ((?i)jpe?g|png|gif)
